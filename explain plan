 delete from RAM_ELIGIBILITY_FILE_DATA df Where    ( SUBSCRIBER_ID,plan_name,product_id,group_id,plan_benefit_pkg,to_char(effective_date,'MMDDYYYY'),to_char(term_date,'MMDDYYYY'))IN (select SUBSCRIBER_ID,plan_name,product_id,group_id,plan_benefit_pkg,effective_date,term_date from RAM_ELIGIBILITY_FILE_ERROR de   where de.SUBSCRIBER_ID = df.SUBSCRIBER_ID  and de.ERROR_REASON IN ('Invalid PlanName','Invalid Demographics Record with Null PlanName') and (de.PLAN_NAME is null or de.plan_name ='DEFAULT')  and de.seq_elig_file_id in(select efhin.seq_elig_file_id  from ram_eligibility_file_header efhin  where efhin.plan_name = $1 and efhin.insert_datetime > TO_DATE($2, 'MM/DD/YYYY')))  and df.seq_elig_file_id in (select efh.seq_elig_file_id  from ram_eligibility_file_header efh  where efh.plan_name = $3 and efh.insert_datetime > TO_DATE($4, 'MM/DD/YYYY'))
